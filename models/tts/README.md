# Text-to-Speech (TTS) ONNX Models

This directory is intended for TTS model files used by the Piper neural TTS engine.

## Expected Models
Place the following files here:
- `en_US-amy-medium.onnx`
- `en_US-amy-medium.onnx.json`

Because of their size, `.onnx` files are ignored by git.
